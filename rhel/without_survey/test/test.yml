- name: Test
  become: true
  become_user: root
  hosts: "rhel9-jump"
  tasks:
    - name: Redis | Select Appstream
      ansible.builtin.dnf:
        name: "@redis:remi-8.2"
        state: present
        update_cache: True